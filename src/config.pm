#!/usr/bin/env perl
# NetBSD.sk bot configuration
# $Id$

use strict;

@config::Ubiqs = ('Ubiq');
@config::Revis = ('Revi');
@config::nicks = ('gerda', 'robik');
@config::channels = ('#testbed', '#NetBSD.sk');

1;
